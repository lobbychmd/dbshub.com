/***********  根据表间关系构造树(json) **********/

var doctree = function (db, config) {
    this.db = db;
    this.config = config;
}

exports.doctree = doctree;

doctree.prototype = {
    constructor: doctree,
    get: function (attr) { 
      
    }
}