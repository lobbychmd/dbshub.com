var Enumerable = require('linq');

exports.metaTable = function (doc) {
    var table = {};
    for (var i in doc.Indexies)
        if (doc.Indexies[i].PrimaryKey) {
            //var
        }
    cols = doc.Columns.slice(10);
    Enumerable.From(doc.cols).ForEach(function (obj) {
        obj.PK = true;
        //console.log(obj.PK);
    });

    return doc;
}
